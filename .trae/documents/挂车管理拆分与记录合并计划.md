# 挂车管理拆分与记录合并计划

## 1. 菜单与路由调整
- **菜单重组**：在 `Layout.vue` 中，将“挂车列表”移动到“车辆管理”子菜单下，并移除独立的“挂车管理”大菜单。
- **路由更新**：在 `router/index.js` 中，将挂车列表的路径更新为 `/vehicles/trailers`（或保持原样但逻辑归属变化），并移除已失效的挂车保险/保养/年审路由。

## 2. 合并记录管理页面
将以下页面升级为通用管理页面（支持车辆和挂车）：
- **保险记录** (`VehicleInsurance.vue`)：增加“资产类型”切换（主车/挂车），支持统一查询和添加。
- **保养记录** (`VehicleMaintenance.vue`)：增加“资产类型”切换，支持统一查询和添加。
- **年审记录** (`VehicleInspection.vue`)：增加“资产类型”切换，支持统一查询和添加。

## 3. 技术实现细节
- **列表查询**：在查询参数中增加 `type` (vehicle/trailer)，默认为 'vehicle'。用户可以切换类型来查看不同的数据，或者默认显示全部（取决于具体实现需求，建议增加类型筛选）。
- **表单适配**：
  - 新增/编辑弹窗中增加“资产类型”单选框。
  - 根据选中的类型，动态切换搜索接口（调用 `getVehicles` 或 `getTrailers`）。
- **数据兼容**：确保后端接口 `getRecords` 能够正确处理合并后的资产类型查询。

## 4. 清理工作
- 删除 `src/pages/trailers/` 目录下多余的记录管理文件 (`TrailerInsurance.vue`, `TrailerMaintenance.vue`, `TrailerInspection.vue`)。
- 将 `TrailerList.vue` 移动至 `src/pages/vehicles/` 目录下（可选，保持目录整洁）。

确认后我将开始实施上述改动。